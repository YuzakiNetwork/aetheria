diff --git a/.gitmodules b/.gitmodules
deleted file mode 100644
index 09e8a65..0000000
--- a/.gitmodules
+++ /dev/null
@@ -1,3 +0,0 @@
-[submodule "vendor/baileys-caller"]
-	path = vendor/baileys-caller
-	url = git@github.com:YuzakiNetwork/baileys-caller-patched.git
diff --git a/package.json b/package.json
index 4a59a11..de9a764 100644
--- a/package.json
+++ b/package.json
@@ -37,8 +37,7 @@
 		"@discordjs/voice": "^0.19.2",
 		"@supabase/supabase-js": "^2.105.4",
 		"axios": "^1.13.5",
-		"baileys": "7.0.0-rc13",
-		"baileys-caller": "file:./vendor/baileys-caller",
+		"baron-baileys-v2": "^1.5.8",
 		"cheerio": "^1.2.0",
 		"discord.js": "^14.26.4",
 		"ffmpeg": "^0.0.4",
diff --git a/src/core/connect.js b/src/core/connect.js
index 4c6ed95..0ce24d6 100644
--- a/src/core/connect.js
+++ b/src/core/connect.js
@@ -32,7 +32,7 @@ import {
 	makeCacheableSignalKeyStore,
 	makeWASocket,
 	proto,
-} from "baileys";
+} from "baron-baileys-v2";
 import qrcode from "qrcode";
 
 /**
@@ -303,15 +303,6 @@ class Connect {
 					`Connection opened successfully for session ${this.sessionName}.`
 				);
 
-				// Register the connected socket with the VoIP call adapter
-				// so baileys-caller can reuse it for voice-call signaling
-				// instead of opening a second linked-device session.
-				try {
-					const { setSharedSock } = await import("#lib/callAdapter");
-					setSharedSock(this.sock);
-				} catch {
-					// callAdapter or baileys-caller not available — skip silently.
-				}
 			}
 		});
 
diff --git a/src/core/message.js b/src/core/message.js
index 7e10bdf..1c56c30 100644
--- a/src/core/message.js
+++ b/src/core/message.js
@@ -24,8 +24,8 @@ class Message {
 
 	/**
 	 * Handle 'messages.upsert' event from Baileys.
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - Baileys socket object.
-	 * @param {{ messages: import('@itsliaaa/baileys').proto.IWebMessageInfo[], type: string }} data - Message data from the event.
+	 * @param {import('baron-baileys-v2').WASocket} sock - Baileys socket object.
+	 * @param {{ messages: import('baron-baileys-v2').proto.IWebMessageInfo[], type: string }} data - Message data from the event.
 	 */
 	async process(sock, { messages, type }) {
 		if (type !== "notify") {
diff --git a/src/lib/auth/mongodb.js b/src/lib/auth/mongodb.js
index fa4fb41..9f2b248 100644
--- a/src/lib/auth/mongodb.js
+++ b/src/lib/auth/mongodb.js
@@ -1,5 +1,5 @@
 import print from "#lib/print";
-import { BufferJSON, WAProto, initAuthCreds } from "baileys";
+import { BufferJSON, WAProto, initAuthCreds } from "baron-baileys-v2";
 import { MongoClient } from "mongodb";
 
 /**
diff --git a/src/lib/auth/state.js b/src/lib/auth/state.js
index 8b1147d..a61c5b4 100644
--- a/src/lib/auth/state.js
+++ b/src/lib/auth/state.js
@@ -1,7 +1,7 @@
 import { MYSQL_CONFIG } from "#config/index";
 import { useMongoDbAuthState } from "#lib/auth/mongodb";
 import logger from "#lib/logger";
-import { useMultiFileAuthState } from "baileys";
+import { useMultiFileAuthState } from "baron-baileys-v2";
 import { readdir, unlink } from "node:fs/promises";
 import { join } from "node:path";
 
diff --git a/src/lib/callAdapter.js b/src/lib/callAdapter.js
deleted file mode 100644
index b1490eb..0000000
--- a/src/lib/callAdapter.js
+++ /dev/null
@@ -1,171 +0,0 @@
-// CallAdapter — thin wrapper around baileys-caller's VoipClient.
-//
-// baileys-caller uses its own internal Baileys socket for signaling,
-// so it needs a separate auth directory (authDir). This adapter:
-//   - lazily imports baileys-caller (so the main bot doesn't pay the
-//     WASM init cost unless a call command is actually issued)
-//   - manages a singleton VoipClient per authDir
-//   - exposes connect(), call(), disconnect(), and activeCall tracking
-import { existsSync, mkdirSync } from "node:fs";
-import { resolve } from "node:path";
-
-let _client = null;
-let _identity = null;
-let _connecting = null;
-let _activeCall = null;
-let _sharedSock = null;
-
-/**
- * Register the bot's main Baileys socket so the VoIP caller can reuse
- * it for signaling. Called once after `connection.update === "open"`.
- *
- * @param {any} sock
- * @returns {void}
- */
-export function setSharedSock(sock) {
-	_sharedSock = sock;
-}
-
-/**
- * Get the currently registered shared socket, if any.
- * @returns {any | null}
- */
-export function getSharedSock() {
-	return _sharedSock;
-}
-
-/**
- * Default auth directory for the VoIP socket. Separate from the main
- * bot's auth_info_baileys/ so the two Baileys sockets don't clash.
- * @returns {string}
- */
-export function defaultVoipAuthDir() {
-	const dir = resolve(process.cwd(), "auth_info_voip");
-	if (!existsSync(dir)) {
-		mkdirSync(dir, { recursive: true });
-	}
-	return dir;
-}
-
-/**
- * Lazily create and connect a VoipClient singleton.
- *
- * Accepts either:
- *   - { sock }: existing connected Baileys socket (shares the bot's
- *     linked-device session — no separate QR pairing)
- *   - { authDir }: separate auth dir for the VoIP socket's own session
- *     (requires pairing a second linked device)
- *
- * @param {{ authDir?: string, sock?: any }} [opts]
- * @returns {Promise<import("baileys-caller").VoipClient>}
- */
-export async function getVoipClient(opts = {}) {
-	// Prefer the shared socket from the main bot unless the caller
-	// explicitly asks for a separate authDir.
-	const useSock = opts.sock || (opts.authDir ? null : _sharedSock);
-	const identity = useSock
-		? "shared-sock"
-		: opts.authDir || defaultVoipAuthDir();
-
-	if (_client && _identity === identity) {
-		return _client;
-	}
-
-	if (_connecting) {
-		return _connecting;
-	}
-
-	_connecting = (async () => {
-		const { VoipClient } = await import("baileys-caller");
-		const config = useSock
-			? { sock: useSock }
-			: { authDir: opts.authDir || defaultVoipAuthDir() };
-		const client = new VoipClient(config);
-		await client.connect();
-		_client = client;
-		_identity = identity;
-		_connecting = null;
-		return client;
-	})();
-
-	return _connecting;
-}
-
-/**
- * Place an outbound voice call to a phone number and stream audio.
- *
- * @param {string} phoneNumber Digits only, e.g. "628123456789".
- * @param {{ audioSource?: string, durationMs?: number, authDir?: string, sock?: any }} [opts]
- * @returns {Promise<import("baileys-caller").ActiveCall>}
- */
-export async function placeCall(phoneNumber, opts = {}) {
-	const client = await getVoipClient({
-		authDir: opts.authDir,
-		sock: opts.sock,
-	});
-
-	if (_activeCall) {
-		throw new Error(
-			"VoIP call already in progress. End the current call first."
-		);
-	}
-
-	const call = await client.call(phoneNumber, {
-		audioSource: opts.audioSource || "silence",
-		durationMs: opts.durationMs,
-	});
-
-	_activeCall = call;
-
-	call.on("ended", () => {
-		_activeCall = null;
-	});
-
-	return call;
-}
-
-/**
- * Get the currently active call, if any.
- * @returns {import("baileys-caller").ActiveCall | null}
- */
-export function getActiveCall() {
-	return _activeCall;
-}
-
-/**
- * End the active call, if any.
- * @returns {void}
- */
-export function endActiveCall() {
-	if (_activeCall) {
-		_activeCall.end();
-		_activeCall = null;
-	}
-}
-
-/**
- * Disconnect the VoIP client entirely (close the signaling socket).
- * @returns {void}
- */
-export function disconnectVoip() {
-	if (_client) {
-		_client.disconnect();
-	}
-	_client = null;
-	_identity = null;
-	_activeCall = null;
-}
-
-/**
- * Normalize a phone-number-like string to digits only.
- * Strips @s.whatsapp.net suffix, + prefix, spaces, dashes.
- *
- * @param {string} input
- * @returns {string}
- */
-export function normalizePhone(input) {
-	return String(input || "")
-		.replace(/@s\.whatsapp\.net$/i, "")
-		.replace(/@lid$/i, "")
-		.replace(/[^\d]/g, "");
-}
diff --git a/src/lib/clonebot/connect.js b/src/lib/clonebot/connect.js
index 1ac3d8f..9cce18c 100644
--- a/src/lib/clonebot/connect.js
+++ b/src/lib/clonebot/connect.js
@@ -14,15 +14,19 @@ import {
 	safeNorm,
 } from "#utils/message";
 import NodeCache from "@cacheable/node-cache";
-import {
+import BaronBaileys, {
 	Browsers,
 	DisconnectReason,
 	fetchLatestBaileysVersion,
 	getAggregateVotesInPollMessage,
 	makeCacheableSignalKeyStore,
 	makeWASocket,
-	proto,
-} from "baileys";
+} from "baron-baileys-v2";
+
+// `proto` isn't a statically-detectable named export on baron-baileys-v2's
+// CJS build (its WAProto module assigns `module.exports = $root` at
+// runtime), so it's pulled off the default import instead.
+const { proto } = BaronBaileys;
 import { randomBytes } from "node:crypto";
 import pino from "pino";
 
diff --git a/src/lib/media.js b/src/lib/media.js
index 47c6033..dcf311a 100644
--- a/src/lib/media.js
+++ b/src/lib/media.js
@@ -11,7 +11,7 @@ export function isMediaMessage(str) {
 }
 
 /**
- * @type {keyof import("@itsliaaa/baileys").proto.Message}
+ * @type {keyof import("baron-baileys-v2").proto.Message}
  */
 export const mimeMap = {
 	imageMessage: "image",
diff --git a/src/lib/print.js b/src/lib/print.js
index 94106ce..abae5df 100644
--- a/src/lib/print.js
+++ b/src/lib/print.js
@@ -38,7 +38,7 @@ const log = (type, message, error = null) => {
 /**
  * Print incoming message to the console.
  * @param {object} m - The serialized message object.
- * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+ * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
  */
 export const print = async (m, store) => {
 	if (!m || m.messageTimestamp === undefined || m.messageTimestamp === null) {
diff --git a/src/lib/serialize.js b/src/lib/serialize.js
index 6d1462d..2045b51 100644
--- a/src/lib/serialize.js
+++ b/src/lib/serialize.js
@@ -3,7 +3,7 @@ import * as Func from "#lib/functions";
 import { getPrefix } from "#lib/prefix";
 import Sticker from "#lib/sticker";
 import { to_audio } from "#utils/converter";
-import {
+import BaronBaileys, {
 	areJidsSameUser,
 	chatModificationToAppPatch,
 	downloadMediaMessage,
@@ -15,8 +15,12 @@ import {
 	getContentType,
 	jidDecode,
 	jidNormalizedUser,
-	proto,
-} from "baileys";
+} from "baron-baileys-v2";
+
+// `proto` isn't a statically-detectable named export on baron-baileys-v2's
+// CJS build (its WAProto module assigns `module.exports = $root` at
+// runtime), so it's pulled off the default import instead.
+const { proto } = BaronBaileys;
 import { fileTypeFromBuffer } from "file-type";
 import { randomBytes } from "node:crypto";
 import { existsSync, promises, readFileSync } from "node:fs";
diff --git a/src/lib/store.js b/src/lib/store.js
index f690e50..0cbf68c 100644
--- a/src/lib/store.js
+++ b/src/lib/store.js
@@ -4,7 +4,7 @@ import {
 	extractMessageContent,
 	getContentType,
 	jidNormalizedUser,
-} from "baileys";
+} from "baron-baileys-v2";
 import { mkdir, readFile, writeFile } from "fs/promises";
 import { MongoClient } from "mongodb";
 import { join } from "path";
diff --git a/src/plugins/ai/gpt.js b/src/plugins/ai/gpt.js
index fb97e20..f3b8a97 100644
--- a/src/plugins/ai/gpt.js
+++ b/src/plugins/ai/gpt.js
@@ -20,7 +20,7 @@ export default {
 
 	/**
 	 * @param {import("../../lib/serialize").default} m
-	 * @param {{ sock: import("@itsliaaa/baileys").WASocket }}
+	 * @param {{ sock: import("baron-baileys-v2").WASocket }}
 	 */
 	execute: async (m) => {
 		let input = m.text?.trim();
diff --git a/src/plugins/audio/callend.js b/src/plugins/audio/callend.js
deleted file mode 100644
index d8c05c9..0000000
--- a/src/plugins/audio/callend.js
+++ /dev/null
@@ -1,18 +0,0 @@
-// .callend — end the current VoIP call if any.
-import { endActiveCall, getActiveCall } from "#lib/callAdapter";
-
-export default {
-	name: "callend",
-	command: ["callend", "stopcall"],
-	hidden: false,
-	description: "Tutup voice call WhatsApp yang sedang berjalan.",
-	usage: ".callend",
-	execute: async (m) => {
-		const active = getActiveCall();
-		if (!active) {
-			return m.reply("Tidak ada call berlangsung.");
-		}
-		endActiveCall();
-		return m.reply("📴 Call ditutup.");
-	},
-};
diff --git a/src/plugins/audio/callmusik.js b/src/plugins/audio/callmusik.js
deleted file mode 100644
index bd3235e..0000000
--- a/src/plugins/audio/callmusik.js
+++ /dev/null
@@ -1,84 +0,0 @@
-// .callmusik — place a WhatsApp voice call to a phone number and
-// play music (YouTube audio) over the call. Requires ffmpeg on PATH
-// and baileys-caller (installed as a peer dependency). The VoIP socket
-// uses a separate auth state directory (auth_info_voip/) so it doesn't
-// conflict with the main bot's WhatsApp socket.
-import { getActiveCall, normalizePhone, placeCall } from "#lib/callAdapter";
-import { downloadYt, isYoutubeUrl } from "#lib/yt-dlp";
-
-const MAX_DURATION_MS = 5 * 60 * 1000;
-const PHONE_USAGE_HINT =
-	"Contoh: .callmusik 628123456789 <youtube-url>\n" +
-	"Nomor phone harus digits only, audio YouTube maks 5 menit.";
-
-export default {
-	name: "callmusik",
-	command: ["callmusik", "callmusic", "musikcall"],
-	hidden: false,
-	description: "Telepon WhatsApp + play musik YouTube via voice call.",
-	usage: ".callmusik <nomor> <youtube-url>",
-	execute: async (m, { args }) => {
-		if (!args || args.length < 2) {
-			return m.reply(PHONE_USAGE_HINT);
-		}
-
-		const phone = normalizePhone(args[0]);
-		const query = args.slice(1).join(" ").trim();
-
-		if (!phone) {
-			return m.reply("Nomor telepon kosong.\n" + PHONE_USAGE_HINT);
-		}
-
-		if (!query) {
-			return m.reply("URL YouTube kosong.\n" + PHONE_USAGE_HINT);
-		}
-
-		const url = isYoutubeUrl(query)
-			? query
-			: `https://www.youtube.com/watch?v=${encodeURIComponent(query)}`;
-
-		if (getActiveCall()) {
-			return m.reply(
-				"Sedang ada call berlangsung. Ketik .callend untuk menutup."
-			);
-		}
-
-		await m.reply(`⏳ Download audio dari YouTube...\n${url}`);
-		let mediaPath;
-		try {
-			mediaPath = await downloadYt(url, {
-				video: false,
-				title: "callmusik",
-			});
-		} catch (error) {
-			return m.reply(
-				`Download YouTube gagal: ${error?.message || error}`
-			);
-		}
-
-		if (!mediaPath) {
-			return m.reply("Download YouTube gagal: file tidak ditemukan.");
-		}
-
-		await m.reply(`📞 Menelpon ${phone}...`);
-		try {
-			const call = await placeCall(phone, {
-				audioSource: mediaPath,
-				durationMs: MAX_DURATION_MS,
-			});
-
-			call.on("connected", () =>
-				m.reply("✅ Call terhubung. Memutar musik...")
-			);
-			call.on("ended", (reason) =>
-				m.reply(`📴 Call berakhir: ${reason || "selesai"}.`)
-			);
-
-			return m.reply(
-				`📞 Call ke ${phone} dihubukan. Musik: ${url}\nKetik .callend untuk menutup.`
-			);
-		} catch (error) {
-			return m.reply(`Gagal menelpon: ${error?.message || error}`);
-		}
-	},
-};
diff --git a/src/plugins/convert/toaudio.js b/src/plugins/convert/toaudio.js
index 4e259f8..e471ce7 100644
--- a/src/plugins/convert/toaudio.js
+++ b/src/plugins/convert/toaudio.js
@@ -19,7 +19,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m) => {
diff --git a/src/plugins/convert/toimage.js b/src/plugins/convert/toimage.js
index 5f8463e..72b875d 100644
--- a/src/plugins/convert/toimage.js
+++ b/src/plugins/convert/toimage.js
@@ -19,7 +19,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m) => {
diff --git a/src/plugins/convert/tovideo.js b/src/plugins/convert/tovideo.js
index 2c21e61..7058fbd 100644
--- a/src/plugins/convert/tovideo.js
+++ b/src/plugins/convert/tovideo.js
@@ -19,7 +19,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m) => {
diff --git a/src/plugins/downloader/spotify.js b/src/plugins/downloader/spotify.js
index a02d8b7..f82130a 100644
--- a/src/plugins/downloader/spotify.js
+++ b/src/plugins/downloader/spotify.js
@@ -21,7 +21,7 @@ export default {
 
 	/**
 	 * @param {import("../../lib/serialize").default} m
-	 * @param {{ api: any, sock: import("@itsliaaa/baileys").WASocket }}
+	 * @param {{ api: any, sock: import("baron-baileys-v2").WASocket }}
 	 */
 	async execute(m, { api, sock }) {
 		if (!sock.spotify) {
@@ -97,7 +97,7 @@ export default {
 	/**
 	 * Handles replies to the search result message.
 	 * @param {import("../../lib/serialize").default} m
-	 * @param {{ sock: import("@itsliaaa/baileys").WASocket, api: any }}
+	 * @param {{ sock: import("baron-baileys-v2").WASocket, api: any }}
 	 */
 	async after(m, { sock, api }) {
 		const session = sock.spotify?.[m.sender];
diff --git a/src/plugins/group/add.js b/src/plugins/group/add.js
index f2de179..fc4666f 100644
--- a/src/plugins/group/add.js
+++ b/src/plugins/group/add.js
@@ -1,5 +1,5 @@
 import { getFile } from "#lib/functions";
-import { WAProto, generateWAMessageFromContent, toNumber } from "baileys";
+import { WAProto, generateWAMessageFromContent, toNumber } from "baron-baileys-v2";
 
 export default {
 	name: "add",
@@ -20,7 +20,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m, { sock }) => {
diff --git a/src/plugins/group/demote.js b/src/plugins/group/demote.js
index fcd9c98..4592d49 100644
--- a/src/plugins/group/demote.js
+++ b/src/plugins/group/demote.js
@@ -17,7 +17,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	async execute(m, { sock, groupMetadata }) {
diff --git a/src/plugins/group/everyone.js b/src/plugins/group/everyone.js
index 856aaf5..fad3da6 100644
--- a/src/plugins/group/everyone.js
+++ b/src/plugins/group/everyone.js
@@ -20,7 +20,7 @@ export default {
 
 	/**
 	 * @param {object} m serialized message
-	 * @param {{ text: string, sock: import('@itsliaaa/baileys').WASocket }} ctx
+	 * @param {{ text: string, sock: import('baron-baileys-v2').WASocket }} ctx
 	 */
 	execute: async (m, { text, sock, groupMetadata }) => {
 		if (!groupMetadata?.participants?.length) {
diff --git a/src/plugins/group/kick.js b/src/plugins/group/kick.js
index c94ffcc..f0a7ea0 100644
--- a/src/plugins/group/kick.js
+++ b/src/plugins/group/kick.js
@@ -17,7 +17,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	async execute(m, { sock, groupMetadata }) {
diff --git a/src/plugins/group/promote.js b/src/plugins/group/promote.js
index dd82180..0f77e35 100644
--- a/src/plugins/group/promote.js
+++ b/src/plugins/group/promote.js
@@ -17,7 +17,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	async execute(m, { sock, groupMetadata }) {
diff --git a/src/plugins/group/setppgc.js b/src/plugins/group/setppgc.js
index e7a91c0..d45f026 100644
--- a/src/plugins/group/setppgc.js
+++ b/src/plugins/group/setppgc.js
@@ -1,4 +1,4 @@
-import { S_WHATSAPP_NET } from "baileys";
+import { S_WHATSAPP_NET } from "baron-baileys-v2";
 import { Jimp, JimpMime } from "jimp";
 
 export default {
@@ -20,7 +20,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m, { sock }) => {
diff --git a/src/plugins/info/login.js b/src/plugins/info/login.js
index d5a4e92..1af95be 100644
--- a/src/plugins/info/login.js
+++ b/src/plugins/info/login.js
@@ -4,7 +4,7 @@ import {
 	createAccountLinkSession,
 	getLinkedAccountByWhatsApp,
 } from "#lib/supabase/accountLinking";
-import { WAProto, generateWAMessageFromContent } from "baileys";
+import { WAProto, generateWAMessageFromContent } from "baron-baileys-v2";
 
 const USE_EXPERIMENTAL_CTA_URL =
 	process.env.AETHERIA_WA_CTA_URL_ENABLED === "true";
diff --git a/src/plugins/info/ping.js b/src/plugins/info/ping.js
index 9e86f1f..3696856 100644
--- a/src/plugins/info/ping.js
+++ b/src/plugins/info/ping.js
@@ -20,7 +20,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	// can like this, async execute(m, { property }) {..} or async execute(m) {..}
diff --git a/src/plugins/misc/delmsg.js b/src/plugins/misc/delmsg.js
index a2d93f5..f048ed3 100644
--- a/src/plugins/misc/delmsg.js
+++ b/src/plugins/misc/delmsg.js
@@ -17,7 +17,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m, { isAdmin, isOwner, isBotAdmin }) => {
diff --git a/src/plugins/misc/quoted.js b/src/plugins/misc/quoted.js
index 734fca4..df887c8 100644
--- a/src/plugins/misc/quoted.js
+++ b/src/plugins/misc/quoted.js
@@ -13,7 +13,7 @@ export default {
 	react: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m) => {
diff --git a/src/plugins/misc/rvo.js b/src/plugins/misc/rvo.js
index 7e78aac..056c9c4 100644
--- a/src/plugins/misc/rvo.js
+++ b/src/plugins/misc/rvo.js
@@ -17,7 +17,7 @@ export default {
 	owner: false,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m, { sock, args }) => {
diff --git a/src/plugins/owner/clearchat.js b/src/plugins/owner/clearchat.js
index 96793b4..cb32a30 100644
--- a/src/plugins/owner/clearchat.js
+++ b/src/plugins/owner/clearchat.js
@@ -17,7 +17,7 @@ export default {
 	owner: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m, { sock }) => {
diff --git a/src/plugins/owner/clonebot.js b/src/plugins/owner/clonebot.js
index f4cd606..be66503 100644
--- a/src/plugins/owner/clonebot.js
+++ b/src/plugins/owner/clonebot.js
@@ -15,7 +15,7 @@ export default {
 	/**
 	 * @param {import("../../lib/serialize.js").SerializedMessage} m
 	 * @param {string} text
-	 * @param {import("@itsliaaa/baileys").WASocket} sock
+	 * @param {import("baron-baileys-v2").WASocket} sock
 	 */
 	execute: async (m, { text, sock }) => {
 		const phone = (text || "").replace(/\D/g, "");
diff --git a/src/plugins/owner/delfile.js b/src/plugins/owner/delfile.js
index 5cb963e..3fc3997 100644
--- a/src/plugins/owner/delfile.js
+++ b/src/plugins/owner/delfile.js
@@ -20,7 +20,7 @@ export default {
 	owner: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m) => {
diff --git a/src/plugins/owner/eval.js b/src/plugins/owner/eval.js
index 6afc6cd..8ae8c6c 100644
--- a/src/plugins/owner/eval.js
+++ b/src/plugins/owner/eval.js
@@ -17,7 +17,7 @@ export default {
 	/**
 	 * Executes the provided JavaScript code.
 	 * @param {object} context - The context object.
-	 * @param {import('@itsliaaa/baileys').WASocket} context.sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} context.sock - The Baileys socket object.
 	 * @param {object} context.m - The serialized message object.
 	 * @param {string} context.text - The text content after the command.
 	 * @param {string[]} context.args - Arguments of the command.
diff --git a/src/plugins/owner/exec.js b/src/plugins/owner/exec.js
index 0517aff..97953c1 100644
--- a/src/plugins/owner/exec.js
+++ b/src/plugins/owner/exec.js
@@ -17,7 +17,7 @@ export default {
 	/**
 	 * Executes the provided JavaScript code.
 	 * @param {object} context - The context object.
-	 * @param {import('@itsliaaa/baileys').WASocket} context.sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} context.sock - The Baileys socket object.
 	 * @param {object} context.m - The serialized message object.
 	 */
 	async execute(m, { isOwner, text }) {
diff --git a/src/plugins/owner/getplug.js b/src/plugins/owner/getplug.js
index a8daf70..36e82e1 100644
--- a/src/plugins/owner/getplug.js
+++ b/src/plugins/owner/getplug.js
@@ -19,7 +19,7 @@ export default {
 	owner: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 * @param {object} plugins - The plugins object.
 	 * @param {object} args - The arguments object.
diff --git a/src/plugins/owner/join.js b/src/plugins/owner/join.js
index 46c2ccf..b9885e2 100644
--- a/src/plugins/owner/join.js
+++ b/src/plugins/owner/join.js
@@ -17,7 +17,7 @@ export default {
 	owner: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock
+	 * @param {import('baron-baileys-v2').WASocket} sock
 	 * @param {object} m
 	 */
 	async execute(m, { sock, text }) {
diff --git a/src/plugins/owner/ostorygc.js b/src/plugins/owner/ostorygc.js
index b0cf3b2..8d6dc02 100644
--- a/src/plugins/owner/ostorygc.js
+++ b/src/plugins/owner/ostorygc.js
@@ -20,7 +20,7 @@ export default {
 
 	/**
 	 * @param {import("../../lib/serialize").default} m
-	 * @param {{ sock: import("@itsliaaa/baileys").WASocket, text: string }}
+	 * @param {{ sock: import("baron-baileys-v2").WASocket, text: string }}
 	 */
 	async execute(m, { sock, text }) {
 		if (!sock.swgc) {
diff --git a/src/plugins/owner/savefile.js b/src/plugins/owner/savefile.js
index e7dda40..f9eafc4 100644
--- a/src/plugins/owner/savefile.js
+++ b/src/plugins/owner/savefile.js
@@ -19,7 +19,7 @@ export default {
 	owner: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	execute: async (m) => {
diff --git a/src/plugins/owner/setpp.js b/src/plugins/owner/setpp.js
index 8aebb7f..28028e6 100644
--- a/src/plugins/owner/setpp.js
+++ b/src/plugins/owner/setpp.js
@@ -1,4 +1,4 @@
-import { S_WHATSAPP_NET } from "baileys";
+import { S_WHATSAPP_NET } from "baron-baileys-v2";
 import { Jimp, JimpMime } from "jimp";
 
 export default {
@@ -20,7 +20,7 @@ export default {
 	owner: true,
 
 	/**
-	 * @param {import('@itsliaaa/baileys').WASocket} sock - The Baileys socket object.
+	 * @param {import('baron-baileys-v2').WASocket} sock - The Baileys socket object.
 	 * @param {object} m - The serialized message object.
 	 */
 	async execute(m, { sock }) {
diff --git a/src/plugins/tools/getsw.js b/src/plugins/tools/getsw.js
index 40caec5..4e50aa7 100644
--- a/src/plugins/tools/getsw.js
+++ b/src/plugins/tools/getsw.js
@@ -29,7 +29,7 @@ export default {
 	 *
 	 * @param {object} m - Serialized message object.
 	 * @param {object} context - Command context.
-	 * @param {import('@itsliaaa/baileys').WASocket} context.sock - Active Baileys socket.
+	 * @param {import('baron-baileys-v2').WASocket} context.sock - Active Baileys socket.
 	 * @returns {Promise<string>}
 	 */
 	execute: async (m, { sock }) => {
diff --git a/src/plugins/tools/lyrics.js b/src/plugins/tools/lyrics.js
index 0199168..b5808a7 100644
--- a/src/plugins/tools/lyrics.js
+++ b/src/plugins/tools/lyrics.js
@@ -18,7 +18,7 @@ export default {
 
 	/**
 	 * @param {import("../../lib/serialize").default} m
-	 * @param {{ api: any, sock: import("@itsliaaa/baileys").WASocket }}
+	 * @param {{ api: any, sock: import("baron-baileys-v2").WASocket }}
 	 */
 	async execute(m, { api, sock }) {
 		if (!sock.lyrics) {
@@ -72,7 +72,7 @@ export default {
 	/**
 	 * Handles replies to the search result message.
 	 * @param {import("../../lib/serialize").default} m
-	 * @param {{ sock: import("@itsliaaa/baileys").WASocket, api: any }}
+	 * @param {{ sock: import("baron-baileys-v2").WASocket, api: any }}
 	 */
 	async after(m, { sock, api }) {
 		const session = sock.lyrics?.[m.sender];
diff --git a/src/utils/message.js b/src/utils/message.js
index f23591b..1ae6821 100644
--- a/src/utils/message.js
+++ b/src/utils/message.js
@@ -3,7 +3,7 @@ import {
 	getContentType,
 	jidNormalizedUser,
 	proto,
-} from "baileys";
+} from "baron-baileys-v2";
 import { createHash } from "node:crypto";
 
 const MESSAGE_EDIT = proto?.Message?.ProtocolMessage?.Type?.MESSAGE_EDIT ?? 14;
diff --git a/tests/callAdapter.test.js b/tests/callAdapter.test.js
deleted file mode 100644
index 88e54a1..0000000
--- a/tests/callAdapter.test.js
+++ /dev/null
@@ -1,38 +0,0 @@
-import {
-	defaultVoipAuthDir,
-	endActiveCall,
-	getActiveCall,
-	normalizePhone,
-} from "#lib/callAdapter";
-import assert from "node:assert/strict";
-import test from "node:test";
-
-test("normalizePhone strips @s.whatsapp.net suffix", () => {
-	assert.equal(normalizePhone("628123456789@s.whatsapp.net"), "628123456789");
-});
-
-test("normalizePhone removes non-digits", () => {
-	assert.equal(normalizePhone("+62 812-345-6789"), "628123456789");
-});
-
-test("normalizePhone handles empty input", () => {
-	assert.equal(normalizePhone(""), "");
-	assert.equal(normalizePhone(null), "null" === String(null) ? "" : "");
-	assert.equal(normalizePhone(undefined), "");
-});
-
-test("defaultVoipAuthDir returns a path string", () => {
-	const dir = defaultVoipAuthDir();
-	assert.ok(typeof dir === "string");
-	assert.ok(dir.length > 0);
-	assert.match(dir, /auth_info_voip$/);
-});
-
-test("getActiveCall returns null when no call is placed", () => {
-	assert.equal(getActiveCall(), null);
-});
-
-test("endActiveCall is a no-op when no call is placed", () => {
-	endActiveCall();
-	assert.equal(getActiveCall(), null);
-});
diff --git a/vendor/baileys-caller b/vendor/baileys-caller
deleted file mode 160000
index 4cacb56..0000000
--- a/vendor/baileys-caller
+++ /dev/null
@@ -1 +0,0 @@
-Subproject commit 4cacb56232e964d412f866a08c4ae686b67d00ca
